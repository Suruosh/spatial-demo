// XR feature — optional enhancement layer (@react-three/xr).
// XR is enhancement, not the product: the full experience must work without it
// (BRAIN.md → "XR Philosophy"). Build web-first, then XR-compatible.
export {};
